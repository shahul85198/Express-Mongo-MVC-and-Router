/*const user = {
    checkemailisexist: function(email) {
        return `select * from uers where email=${email}`
    },

    checklogin: function(email, password) {
        return `select * from users where email=${email} and password=${password}`
    }
}


module.exports = {
    user: user 
}

//<!-- user.controller.js -->
const isValid = user.checkemailisexist("syed@gmail")
*/